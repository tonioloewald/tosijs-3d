# TODO

tosijs-3d's tasks live on the **virta board**, not in this file. An agent
session in this repo opens with `virta brief` (a SessionStart hook runs it);
`virta ls "project:tosijs-3d status:ready"` is what is cleared to work on,
and `virta create "…" --project tosijs-3d` files something new, with a body
saying why it matters and what done looks like.

The board: https://virta.tosijs.net/host/#?virta.scope=tosijs-3d

This file used to be the worklist and the design journal at once. The journal
is `DECISIONS.md` now, verbatim, for the reasoning behind past decisions.
Keep this file prose: a list item here would be imported as a task.
