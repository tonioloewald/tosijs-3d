# Upstream

Ecosystem findings that belong in another repo (`tosijs`, `tosijs-ui`, `tjs-lang`, the build
system) — recurring footguns tosijs-3d works around. **File, don't fix**: never edit the upstream
repo from here; open an issue there and mirror its URL below. Surfaced by the 0.5.0 nine-lens
review; none block a release, but they compound (this release re-hand-guarded the same footguns
across four new components).

| Repo                               | Finding                                                                                                                                                                                                                                                                                                                                                                                                            | Issue                                                             |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| `tonioloewald/tosijs`              | **`elementCreator` should warn when a declared class-field prop collides with the `on*`→`addEventListener` binding.** The single most-repeated tax: `b3d-controller.ts`, `b3d-destroyable.ts`, `b3d-spawner.ts`, `b3d-death.ts` each carry an identical "deliberately NOT named `onX`" guard + comment. A definition-time warning would delete the whole class of silent-null-callback bugs.                       | [tosijs#14](https://github.com/tonioloewald/tosijs/issues/14)     |
| `tonioloewald/tosijs`              | **`foo: true` in `initAttributes` should be a definition-time error.** ✅ **Now enforced** — tosijs THROWS at construction on a true-default boolean. This surfaced (and hard-crashed) `b3d-controller`'s latent `player: true`, since fixed. Only nit left: the throw is at _construction_, not _definition_ — so a never-instantiated offender still slips a build; and see the haltija row below (the throw was invisible to `hj console`).                       | [tosijs#15](https://github.com/tonioloewald/tosijs/issues/15)     |
| `tonioloewald/tosijs`              | **Surface a first-class "semantic parent" accessor** so a nested child needn't know about the `<tosi-slot>` wrapper. `semanticParent()` is the current stopgap, hand-rolled here.                                                                                                                                                                                                                                  | [tosijs#16](https://github.com/tonioloewald/tosijs/issues/16)     |
| `tonioloewald/tjs-lang`            | **All-caps identifier reassignment is rewritten to `const`,** shadowing a module-level `let` (bit the exploder/physics demos).                                                                                                                                                                                                                                                                                     | [tjs-lang#22](https://github.com/tonioloewald/tjs-lang/issues/22) |
| `@babylonjs/loaders` (third-party) | **`babylonjs-gltf2interface` is pulled into the dep graph purely to satisfy `@babylonjs/loaders@9`'s type peer** for the emitted `.d.ts`. A types-only package promoted to a peer pushes resolution down the whole consumer chain. **Not filed** — it's a third-party (Babylon) packaging choice, not our repos, and it's already handled locally (parked in `devDependencies`). Recorded here for awareness only. | n/a (handled locally)                                             |
| `tonioloewald/haltija`             | **Tab selection follows focus, not cwd.** Directory routing picks the right _server_, then stops: from this repo's cwd an `hj eval` landed on a `tosijs-ui` page. Two agents each correctly staying in their own directory can still drive each other's pages. Observed on 1.4.0; may be covered by the cwd-routing work landing in 1.4.1.                                                                         | [haltija#2](https://github.com/tonioloewald/haltija/issues/2)     |
| `tonioloewald/haltija`             | **A hidden/off-screen tab is indistinguishable from a broken page** — rAF + IntersectionObserver throttle, so the doc browser never mounts its live example and `querySelector('tosi-b3d')` returns 0 indefinitely. Filed with the repro their commit asked for; `lastSeen` age already carries the signal, it's just unlabelled.                                                                                  | [haltija#3](https://github.com/tonioloewald/haltija/issues/3)     |
| `tonioloewald/haltija`             | **`hj console` captures `console.*` but NOT uncaught exceptions.** Cost hours on `b3d-controller`: the component was dead because its constructor threw (`player: true`), but the Uncaught Error only showed in real DevTools — `hj console` showed the `console.log` marker right next to it, never the throw. An agent driving a page can't see the one error that matters. Capture `window.onerror`/`unhandledrejection` too. **To file.** | to file                                                          |
| `tonioloewald/haltija`             | **Programmatic affordances for tosijs-ui live examples.** Debugging via the live example (toggle view/edit-code, edit, Refresh) is powerful but agent-driven access means clicking through the example menu's DOM (`button.source-menu` → `.xin-menu-item`). First-class `hj` verbs (or documented selectors) for: toggle edit mode, refresh/re-run an example, set example code. Note the caveat worth documenting: Refresh re-runs against the **already-loaded ES module**, so *source* changes need a full reload; only *example-code* edits take effect via Refresh. **To file / document.** | to file                                                          |

## Incoming

`gh issue list -R tonioloewald/tosijs-3d` is empty (open and closed). 0.5.0 closes no incoming
issue and is not reframed by any downstream port.
