/** What counts as interesting: a planet with HI at or below this. */
export declare const INTERESTING_MAX_HI = 2;
/** Corpus size per cell. */
export declare const CORPUS_PER_CELL = 5000;
/** The HI rules these rates were measured against — see hiFingerprint. */
export declare const HI_FINGERPRINT = "82da5176";
/** Fraction of each cell's systems that are interesting. */
export declare const PASS_RATE: Readonly<Record<string, number>>;
//# sourceMappingURL=population-table.d.ts.map